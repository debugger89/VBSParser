Function ShowSum(ByRef  value1, value2)

	For counter = start To end stepcount
    	MsgBox "IN FOR"
    Next
	
	If a > 0 Then
	   Document.write "a is a POSITIVE Number"
	ElseIf a < 2 Then
	   Document.write "a is a NEGATIVE Number"
	Else
	   Document.write "a is EQUAL than ZERO"
	End If
	
End Function
